import React from 'react';

import './Title.css';

const Title = () => {
	return (
		<div className="title">
			<span className="title-text">React ToDo List</span>
		</div>
	)
};

export default Title;
